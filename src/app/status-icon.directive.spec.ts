import { StatusIconDirective } from './status-icon.directive';

describe('StatusIconDirective', () => {
  it('should create an instance', () => {
    const directive = new StatusIconDirective();
    expect(directive).toBeTruthy();
  });
});
