import { TaskHighlighterDirective } from './task-highlighter.directive';

describe('TaskHighlighterDirective', () => {
  it('should create an instance', () => {
    const directive = new TaskHighlighterDirective();
    expect(directive).toBeTruthy();
  });
});
